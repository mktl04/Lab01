@model Cart
<section class="bg-light my-5">
    <div class="container">
        <div class="row">
            <div class="col-lg-9">
                <div class="card border shadow-0">
                    <div class="m-4">
                        <h4 class="card-title mb-4">Giỏ hàng của bạn</h4>
                        @foreach (var item in Model.Items)
                        {
                            <form asp-action="Update" method="post">
                                <input type="hidden" name="productId" value="@item.Product.Id" />
                                <div class="row gy-3 mb-4">
                                    <div class="col-lg-5">
                                        <div class="me-lg-5">
                                            <div class="d-flex">
                                                <img src="/@item.Product.ImageUrl" class="border rounded me-3" style="width: 96px; height: 96px;" />
                                                <div class="">
                                                    <a href="#" class="nav-link">@item.Product.Name</a>
                                                </div>

                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-lg-2 col-sm-6 col-6 d-flex flex-row flex-lg-column flex-xl-row text-nowrap">
                                        <div class="">
                                            <input type="number" name="quanty" value="@item.Quantity" class="form-control" min="1" />
                                        </div>
                                        <div class="">
                                            <text class="h6">@string.Format("${0:#,##0}", item.Product.Price)</text> <br />
                                        </div>
                                    </div>

                                    <div class="col-lg col-sm-6 d-flex justify-content-sm-center justify-content-md-start justify-content-lg-center justify-content-xl-end mb-2">

                                        <div class="float-md-end">
                                            <button type="submit" class="btn btn-light border text-primary icon-hover-primary"> Cập nhật </button>
                                            <a asp-action="Remove" asp-controller="Cart" asp-route-productId="@item.Product.Id" class="btn btn-light border text-danger icon-hover-danger"> Xóa </a>

                                        </div>
                                    </div>
                                </div>
                            </form>
                         }
                    </div>
                </div>
            </div>
            <div class="col-lg-3">
                <div class="card shadow-0 border">
                    <div class="card-body">
                        <div class="d-flex justify-content-between">
                            <p class="mb-2">Tổng giá tiền:</p>
                            <p class="mb-2">@string.Format("${0:#,##0}", Model.Total)</p>
                        </div>
                        <div class="d-flex justify-content-between">
                            <p class="mb-2">Giảm giá:</p>
                            <p class="mb-2 text-success">-$0</p>
                        </div>
                        <div class="d-flex justify-content-between">
                            <p class="mb-2">Thuế:</p>
                            <p class="mb-2">$0.00</p>
                        </div>
                        <hr />
                        <div class="d-flex justify-content-between">
                            <p class="mb-2">Thành tiền:</p>
                            <p class="mb-2 fw-bold">@string.Format("${0:#,##0}", Model.Total)</p>
                        </div>
                        <div class="mt-3">
                            <a href="#" class="btn btn-success w-100 shadow-0 mb-2"> Thanh toán </a>
                            <a href="/customer" class="btn btn-light w-100 border mt-2"> Quay lại cửa hàng </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>